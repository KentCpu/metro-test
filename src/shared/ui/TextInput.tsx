import { TextInput as UITextInput } from "@mantine/core";
import type { TextInputProps } from "@mantine/core";

export function TextInput(props: TextInputProps) {
  return <UITextInput {...props} />;
}
